import { BlurView } from '@react-native-community/blur';
import React, { useState } from 'react';
import { View, Platform, TouchableWithoutFeedback, Keyboard } from 'react-native';
import { Container, KeyboardView } from '../../../components/selector/SelectStyled';
import { Title } from './CreateClassStyled';
import Modal from 'react-native-modal';

export const ScheduleModal = (props: any) => {

  return (
    <KeyboardView>
      <Title>test</Title>


    </KeyboardView>

  )
}
